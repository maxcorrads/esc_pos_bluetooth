name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.2.8
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart:
  esc_pos_utils:
    git:
      url: git://github.com/maxcorrads/esc_pos_utils.git
      ref: nullable
  # esc_pos_utils:
    # path: ../esc_pos_utils
  flutter_blue: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
